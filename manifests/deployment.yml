apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: users-ms
  name: users-ms
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: users-ms
  template:
    metadata:
      labels:
        app.kubernetes.io/name: users-ms
    spec:
      containers:
        - name: users
          env:
            - name: APP_VERSION
              value: "1.0.0"
          image: docker.io/digilinq/users-microservice
          ports:
            - containerPort: 8080
              name: service
            - containerPort: 8081
              name: management
          resources: {}
      restartPolicy: Always