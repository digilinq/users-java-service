name: Build maven project using reusable workflows
on:
  push:
    branches:
      - 'feature/*'
jobs:
  build-maven-project:
    runs-on: ubuntu-latest
    steps:
      - name: Build maven project
        uses: digilinq/reusable-workflows/.github/workflows/build.yaml@t1
